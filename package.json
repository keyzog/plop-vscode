{
	"name": "plop-path",
	"displayName": "Plop Path",
	"description": "Run plop commands from context menu with path or folder selection",
	"publisher": "keyzog",
	"version": "1.0.0",
	"engines": {
		"vscode": "^1.90.0"
	},
	"categories": [
		"Other"
	],
	"keywords": [
		"plop",
		"generate-react-component",
		"folder templates",
		"create-folder",
		"create-file",
		"react template"
	],
	"repository": {
		"url": "https://github.com/keyzog/plop-vscode"
	},
	"bugs": {
		"url": "https://github.com/keyzog/plop-vscode/issues"
	},
	"license": "MIT",
	"activationEvents": [],
	"icon": "resources/logo.png",
	"main": "./dist/extension.js",
	"contributes": {
		"menus": {
			"explorer/context": [
				{
					"command": "plop-path.openTerminalWithPath",
					"group": "navigation@100"
				}
			]
		},
		"commands": [
			{
				"command": "plop-path.openTerminalWithPath",
				"title": "💧 Run Plop Here..."
			}
		],
		"configuration": [
			{
				"title": "Plop Test",
				"required": [
					"plop-path.plopCommand",
					"plop-path.pathArgName"
				],
				"properties": {
					"plop-path.plopCommand": {
						"type": "string",
						"default": "plopit",
						"description": "The command to run Plop. This can be customized to any Plop command you want to use. By default: `plopit` if you use plopit installed globally or `npx plop --` for base plop.\n\nFor example, if the command is 'plopit', and the path argument name is 'path', the generated command will be `plopit --path <directory_path>`. \n\nCommand template: `${plopCommand} --${pathArgName}  <directory_path>`"
					},
					"plop-path.pathArgName": {
						"type": "string",
						"default": "path",
						"description": "The argument name for the path. This is used to pass the directory path to the Plop command."
					}
				}
			}
		]
	},
	"scripts": {
		"pack": "vsce package --no-dependencies -o ./out",
		"publish": "vsce publish --no-dependencies",
		"vscode:prepublish": "npm run package",
		"compile": "npm run check-types && npm run lint && node esbuild.js",
		"watch": "npm-run-all -p watch:*",
		"watch:esbuild": "node esbuild.js --watch",
		"watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
		"package": "npm run check-types && npm run lint && node esbuild.js --production",
		"compile-tests": "tsc -p . --outDir out",
		"watch-tests": "tsc -p . -w --outDir out",
		"pretest": "npm run compile-tests && npm run compile && npm run lint",
		"check-types": "tsc --noEmit",
		"lint": "eslint src --ext ts",
		"test": "vscode-test"
	},
	"devDependencies": {
		"@types/vscode": "^1.90.0",
		"@types/mocha": "^10.0.6",
		"@types/node": "20.x",
		"@typescript-eslint/eslint-plugin": "^7.11.0",
		"@typescript-eslint/parser": "^7.11.0",
		"eslint": "^8.57.0",
		"esbuild": "^0.21.5",
		"npm-run-all": "^4.1.5",
		"typescript": "^5.4.5",
		"@vscode/test-cli": "^0.0.9",
		"@vscode/test-electron": "^2.4.0"
	}
}
